COMPANY_NAME="Vercel Inc."
SITE_NAME="Next.js Commerce"
SHOPIFY_REVALIDATION_SECRET=""
SHOPIFY_STOREFRONT_ACCESS_TOKEN=""
SHOPIFY_STORE_DOMAIN="[your-shopify-store-subdomain].myshopify.com"

# SoundCloud playlist embedded in the homepage radio player.
NEXT_PUBLIC_SOUNDCLOUD_PLAYLIST_URL=""

# Meta (Facebook / Instagram) Pixel.
# Find this in Meta Events Manager -> Data Sources -> your pixel.
# Leave empty to disable tracking entirely: no scripts load and no events fire.
# For production, set the same variable in
# Vercel -> Project -> Settings -> Environment Variables.
NEXT_PUBLIC_META_PIXEL_ID=""

# Canonical site origin (no trailing slash). Used for canonical tags,
# sitemap, robots, and structured data.
NEXT_PUBLIC_SITE_URL="https://agmnt.space"

# Wikidata item URL for AGMNT (paste yours, e.g. https://www.wikidata.org/wiki/Q…).
# Emitted in Organization sameAs to consolidate the brand entity. Optional.
NEXT_PUBLIC_WIKIDATA_URL=""
